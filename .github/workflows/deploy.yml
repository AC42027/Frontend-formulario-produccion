name: Deploy Inspecciones Producción

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Pull latest changes
        run: |
          cd /var/www/html/insp_pry/inspecciones
          git pull origin main
